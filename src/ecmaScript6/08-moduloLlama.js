// este archivo debe importar el nombre (hello) de la función
// que está en ./x posición.
// en caso de que la función no exporte como default
// se debe meter en llaves --> {hello}

import hello from "./08-moduloBase.js";

// se llama a la function y retornara lo que contenga, si es el caso.
hello();